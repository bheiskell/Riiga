<?php

class PendingCharacterFixture extends CakeTestFixture {
  var $name = 'PendingCharacter';
  var $import = array('table' => 'pending_characters', 'import' => false);
  var $records = array(
  );
}

?>
